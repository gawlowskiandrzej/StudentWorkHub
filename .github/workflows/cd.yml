name: CD - Deploy to Production

#on:
#  push:
#    branches: [ main ]
#    tags:
#      - 'v*.*.*'
#  workflow_dispatch:
#    inputs:
#      environment:
#        description: 'Environment to deploy to'
#        required: true
#        type: choice
#        options:
#          - production
#          - staging
#
#env:
#  REGISTRY: ghcr.io
#  IMAGE_NAME: ${{ github.repository }}
#
#jobs:
#  build-and-push:
#    name: Build and Push Docker Images
#    runs-on: ubuntu-latest
#    permissions:
#      contents: read
#      packages: write
#    
#    strategy:
#      matrix:
#        component:
#          - name: frontend
#            context: ./frontend
#            dockerfile: ./frontend/Dockerfile
#          - name: offer_manager
#            context: ./offer_collector
#            dockerfile: ./offer_collector/Dockerfile.offer_manager
#          - name: worker
#            context: ./offer_collector
#            dockerfile: ./offer_collector/Dockerfile.worker
#    
#    steps:
#      - uses: actions/checkout@v4
#      
#      - name: Set up Docker Buildx
#        uses: docker/setup-buildx-action@v3
#      
#      - name: Log in to Container Registry
#        uses: docker/login-action@v3
#        with:
#          registry: ${{ env.REGISTRY }}
#          username: ${{ github.actor }}
#          password: ${{ secrets.GITHUB_TOKEN }}
#      
#      - name: Extract metadata
#        id: meta
#        uses: docker/metadata-action@v5
#        with:
#          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/${{ matrix.component.name }}
#          tags: |
#            type=ref,event=branch
#            type=ref,event=pr
#            type=semver,pattern={{version}}
#            type=semver,pattern={{major}}.{{minor}}
#            type=sha,prefix={{branch}}-
#            type=raw,value=latest,enable={{is_default_branch}}
#      
#      - name: Build and push Docker image
#        uses: docker/build-push-action@v5
#        with:
#          context: ${{ matrix.component.context }}
#          file: ${{ matrix.component.dockerfile }}
#          push: true
#          tags: ${{ steps.meta.outputs.tags }}
#          labels: ${{ steps.meta.outputs.labels }}
#          cache-from: type=gha
#          cache-to: type=gha,mode=max
#          platforms: linux/amd64,linux/arm64
#
#  deploy-production:
#    name: Deploy to Production
#    runs-on: ubuntu-latest
#    needs: build-and-push
#    if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v')
#    environment:
#      name: production
#      url: https://studentworkhub.com
#    
#    steps:
#      - uses: actions/checkout@v4
#      
#      - name: Deploy to production server
#        uses: appleboy/ssh-action@v1.0.0
#        with:
#          host: ${{ secrets.PROD_SERVER_HOST }}
#          username: ${{ secrets.PROD_SERVER_USER }}
#          key: ${{ secrets.PROD_SERVER_SSH_KEY }}
#          script: |
#            cd /opt/StudentWorkHub
#            
#            # Pull latest changes
#            git pull origin main
#            
#            # Login to GitHub Container Registry
#            echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin
#            
#            # Pull latest images
#            docker compose -f docker-compose-prod.yml pull
#            
#            # Stop and remove old containers
#            docker compose -f docker-compose-prod.yml down
#            
#            # Start new containers
#            docker compose -f docker-compose-prod.yml up -d
#            
#            # Wait for services to be healthy
#            sleep 10
#            
#            # Check if services are running
#            docker compose -f docker-compose-prod.yml ps
#            
#            # Cleanup old images
#            docker image prune -f
#      
#      - name: Health check
#        run: |
#          sleep 30
#          curl -f https://studentworkhub.com/api/health || exit 1
#      
#      - name: Notify deployment success
#        if: success()
#        run: |
#          echo "✅ Deployment to production successful!"
#          echo "Version: ${{ github.sha }}"
#          echo "Deployed at: $(date)"
#
#  deploy-staging:
#    name: Deploy to Staging
#    runs-on: ubuntu-latest
#    needs: build-and-push
#    if: github.event.inputs.environment == 'staging'
#    environment:
#      name: staging
#      url: https://staging.studentworkhub.com
#    
#    steps:
#      - uses: actions/checkout@v4
#      
#      - name: Deploy to staging server
#        uses: appleboy/ssh-action@v1.0.0
#        with:
#          host: ${{ secrets.STAGING_SERVER_HOST }}
#          username: ${{ secrets.STAGING_SERVER_USER }}
#          key: ${{ secrets.STAGING_SERVER_SSH_KEY }}
#          script: |
#            cd /opt/StudentWorkHub
#            git pull origin develop
#            echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin
#            docker compose pull
#            docker compose down
#            docker compose up -d
#            docker compose ps
#            docker image prune -f
#
#  rollback:
#    name: Rollback Production
#    runs-on: ubuntu-latest
#    if: failure() && (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v'))
#    needs: deploy-production
#    
#    steps:
#      - name: Rollback deployment
#        uses: appleboy/ssh-action@v1.0.0
#        with:
#          host: ${{ secrets.PROD_SERVER_HOST }}
#          username: ${{ secrets.PROD_SERVER_USER }}
#          key: ${{ secrets.PROD_SERVER_SSH_KEY }}
#          script: |
#            cd /opt/StudentWorkHub
#            
#            # Get previous commit
#            PREVIOUS_COMMIT=$(git log --skip=1 --max-count=1 --format="%H")
#            
#            # Checkout previous version
#            git checkout $PREVIOUS_COMMIT
#            
#            # Restart with previous version
#            docker compose -f docker-compose-prod.yml down
#            docker compose -f docker-compose-prod.yml up -d
#            
#            echo "⚠️ Rolled back to commit: $PREVIOUS_COMMIT"
